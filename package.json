{
  "name": "blackbird-landing",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/optimize-product-gallery.mjs && tsc && vite build",
    "preview": "vite preview",
    "optimize-gallery": "node scripts/optimize-product-gallery.mjs"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "~5.7.2",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "page-flip": "^2.0.7"
  }
}
