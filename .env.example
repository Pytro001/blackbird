# Stripe Checkout (Vercel → Environment Variables; use Test keys in Preview if needed)
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=

# Optional: open this link when Buy is clicked if the serverless API is not reachable (e.g. `vite` without `vercel dev`)
# VITE_STRIPE_PAYMENT_LINK=https://buy.stripe.com/test_...

# WhatsApp scalp check (digits only, country code included, no + or spaces)
VITE_WHATSAPP_NUMBER=
# VITE_WHATSAPP_MESSAGE=Hello blackbird® — ...
