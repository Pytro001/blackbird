# Stripe Checkout (Vercel → Environment Variables; use Test keys in Preview if needed)
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=

# Optional: overrides DEFAULT_STRIPE_PAYMENT_LINK in src/main.ts when set
# VITE_STRIPE_PAYMENT_LINK=https://buy.stripe.com/...

# WhatsApp Dermatologist link is set in src/main.ts (WHATSAPP_CHAT_URL).
